export type StatusProjeto = 'ATIVO' | 'PAUSADO' | 'CONCLUIDO' | 'CANCELADO';

export const StatusProjeto = {
  ATIVO: 'ATIVO' as StatusProjeto,
  PAUSADO: 'PAUSADO' as StatusProjeto,
  CONCLUIDO: 'CONCLUIDO' as StatusProjeto,
  CANCELADO: 'CANCELADO' as StatusProjeto
};

